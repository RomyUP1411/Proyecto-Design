{
  "framework": "vite",
  "buildCommand": "cd inventario-bodega && npm install && npm run build && rm -rf ../dist && cp -R dist ../dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
